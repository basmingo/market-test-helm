apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port.kafka.internal }}
      targetPort: {{ .Values.port.kafka.internal }}
      protocol: TCP
